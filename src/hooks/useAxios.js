import axios from 'axios';
import { useCallback, useState } from 'react';

export const METHOD = {
    GET: 'get',
    POST: 'post',
    PUT: 'put',
    DELETE: 'delete',
    PATCH: 'patch',
};

const useAxios = () => {};

export default useAxios;
